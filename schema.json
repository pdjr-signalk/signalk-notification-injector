{	
	"type": "object",
	"properties": {
		"fifo": {
            "title": "FIFO named pipe",
            "type": "string",
            "default": "/var/signalk-injector"
		},
		"passwords": {
			"title": "Access passwords",
			"type": "string",
			"default": ""
		},
        "defaultpath": {
            "title": "Default notification path",
            "type": "string",
            "default": "notifications.injected"
        }
	}
}
